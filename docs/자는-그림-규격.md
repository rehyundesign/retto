# 자는 그림 규격 (받을 것)

레토가 잘 때 쓰는 아틀라스 행 11 이다. 현재는 레토 사진을 참고해 새로 만든 누운 수면
6프레임을 쓰며, 얼굴·발·바닥선은 고정하고 등과 흉곽만 위아래로 숨 쉰다.

## 규격

| 항목 | 값 |
|---|---|
| 파일 | PNG, 투명 배경(알파) |
| 크기 | **1536 × 208** |
| 칸 | 192 × 208 **여섯 칸**, 가로로 이어 붙임 |
| 내용 | 같은 포즈의 숨쉬기 한 주기(들이쉬고 내쉬기). 첫 칸과 마지막 칸이 이어져야 반복이 매끄럽다 |
| 발 위치 | 칸 아래에서 약 8px 위. **여섯 칸 모두 발이 같은 선**에 있어야 떨지 않는다 |
| 눈 | 감은 상태 |
| 자세 | 누워 자도 되고 앉아서 졸아도 된다. 폭 192px 안에 들어와야 한다 |
| 색 | 기존 아틀라스와 같은 랙돌(크림 몸 · 회갈색 귀·꼬리) |

여섯 칸이 아니라 **한 장(192×208)만 줘도 된다.** 그 경우 숨쉬기는 코드로 넣는다
(`tools/sprite-sleep.swift` 가 지금 하는 일이다).

## 얹는 방법

```sh
cd apps/retto-pet
swift tools/sprite-sleep.swift assets/spritesheet.webp /tmp/sheet.png --strip 받은그림.png
cwebp -lossless /tmp/sheet.png -o assets/spritesheet.webp
macos/scripts/build.sh --install
```

규격이 다르면 도구가 거부하고 멈춘다. `sheetHeight`(2496)와
`animationCatalog[.sleeping].row`(11)는 이미 맞춰져 있어 건드릴 필요가 없다.
자체 검사가 자는 행이 아틀라스 안에 있는지 확인한다.

## 지금 쓰는 것

행 11 은 누워 자는 레토의 새 이미지 여섯 장이다. 프레임 바닥선은 y=164~165에 고정되고,
등 최고점은 y=53 → 48 → 45 → 42 → 47 → 53으로 움직인다. 앱은 0.46초마다 한 장씩 보여
약 2.76초에 한 번 숨 쉰다.
