# 앱 아이콘

`assets/icon.png`(1024×1024)가 정본이다. `build.sh` 가 `sips` 로 10가지 크기를 뽑아
`RettoIcon.icns` 를 만든다.

- 예전 아이콘은 128px 전신이라 1024 로 늘리면 흐렸다. 지금은 1024 에서 줄이기만 한다
- 머리 하나가 둥글게 닫힌 형태다. 가슴까지 넣으면 아래가 평평하게 잘려 블록처럼 보인다
- `source.png` 는 생성 원본(크로마키 `#0369F3`). 배경은 `remove_chroma_key.py` 로 판다
